$kind: http-request
name: Crear Pregunta Desarrollo Deterministico
url: "{{baseurl}}/api/preguntas"
method: POST
headers:
  Authorization: Bearer {{jwt_token}}
body:
  type: json
  content: |-
    {
      "tipo": "DESARROLLO_DETERMINISTICO",
      "texto": "¿Cuál es la capital de Francia?",
      "respuestaEsperadaExacta": "París",
      "examenId": 2
    }
scripts:
  - type: afterResponse
    code: |-
      // Verificar respuesta
      if (pm.response.code === 201) {
          console.log("Pregunta creada exitosamente");
      } else {
          console.log("Error al crear pregunta: " + pm.response.code);
      }
    language: text/javascript
order: 5002
