$kind: http-request
name: Crear Pregunta Desarrollo No Deterministico
url: "{{baseurl}}/api/preguntas"
method: POST
headers:
  Authorization: Bearer {{jwt_token}}
body:
  type: json
  content: |-
    {
      "tipo": "DESARROLLO_NO_DETERMINISTICO",
      "texto": "Explica el impacto de la Revolución Francesa",
      "rubricaEvaluacion": "Se evaluará: comprensión histórica, análisis de causas y consecuencias, y claridad en la expresión",
      "examenId": 2
    }
scripts:
  - type: afterResponse
    code: |-
      // Verificar respuesta
      if (pm.response.code === 201) {
          console.log("Pregunta creada exitosamente");
      } else {
          console.log("Error al crear pregunta: " + pm.response.code);
      }
    language: text/javascript
order: 5003
