$kind: http-request
name: Modificar Pregunta Verdadero Falso
url: "{{baseurl}}/api/preguntas/2"
method: PUT
headers:
  Authorization: Bearer {{jwt_token}}
body:
  type: json
  content: |-
    {
      "texto": "¿La Tierra es plana (modificado)?",
      "respuestaCorrecta": false
    }
scripts:
  - type: afterResponse
    code: |-
      // Verificar respuesta
      if (pm.response.code === 200) {
          console.log("Pregunta modificada exitosamente");
      } else {
          console.log("Error al modificar pregunta: " + pm.response.code);
      }
    language: text/javascript
order: 6001
