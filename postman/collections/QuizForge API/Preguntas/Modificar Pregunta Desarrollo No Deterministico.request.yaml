$kind: http-request
name: Modificar Pregunta Desarrollo No Deterministico
url: "{{baseurl}}/api/preguntas/4"
method: PUT
headers:
  Authorization: Bearer {{jwt_token}}
body:
  type: json
  content: |-
    {
      "texto": "Describe los patrones de diseño más importantes (modificado)",
      "rubricaEvaluacion": "Se evaluará la profundidad del análisis y la correcta identificación de patrones"
    }
scripts:
  - type: afterResponse
    code: |-
      // Verificar respuesta
      if (pm.response.code === 200) {
          console.log("Pregunta modificada exitosamente");
      } else {
          console.log("Error al modificar pregunta: " + pm.response.code);
      }
    language: text/javascript
order: 6003
