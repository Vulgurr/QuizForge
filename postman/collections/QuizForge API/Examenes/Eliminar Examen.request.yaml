$kind: http-request
url: "{{baseurl}}/api/examenes/1"
method: DELETE
headers:
  Authorization: Bearer {{jwt_token}}
scripts:
  - type: afterResponse
    code: |-
      // Verificar respuesta
      if (pm.response.code === 204) {
          console.log("Examen eliminado exitosamente");
      } else {
          console.log("Error al eliminar examen: " + pm.response.code);
      }
    language: text/javascript
order: 4000
